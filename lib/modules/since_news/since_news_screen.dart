import 'package:flutter/cupertino.dart';

class SinceNewsScreen extends StatelessWidget {
  const SinceNewsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Since');
  }
}
